name: .NET Core

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
#    - name: Setup .NET Core
#      uses: actions/setup-dotnet@v1
#      with:
#        dotnet-version: 3.1.301
#    - name: Install dependencies
#      run: dotnet restore
#    - name: Test
#      run: dotnet test PrimeService.Tests/PrimeService.Tests.csproj --no-restore
    - name: Fio benchmark
      run: |
        echo "Running FIO"
        fio --list
        fio --randrepeat=1 --direct=1 --gtod_reduce=1 --name=test --bs=128k --iodepth=32 --readwrite=rw --rwmixread=75 --size=200M --ioengine=windowsaio --filename=PhysicalDrive1 --thread=1
      

